from utils_edit import *
from threading import Thread
import random

NUM_NODES = 100
NUM_GAMES = 32000
NUM_THREADS = 32
data = [dict() for _ in range(NUM_THREADS)]

def generate_train(states, idx):
    global data

if __name__ == "__main__":
    with open("data.out", 'r') as file:
        d = file.readlines()
    
    if d == 